# Configurazione Docker per contract-manager-core
# Questo profilo viene attivato quando si esegue con --spring.profiles.active=docker

spring:
  # Configurazione MongoDB per Docker (Replica Set)
  data:
    mongodb:
      uri: mongodb://mongodb-contract:27017/contract?replicaSet=rs-contract

# Configurazione gRPC client per Docker
grpc:
  client:
    customer-manager:
      address: customer-manager-core:9092
      negotiation-type: plaintext
    # pricing-manager non è ancora attivo
    # Manteniamo la configurazione locale che verrà ignorata se il servizio non è disponibile
    pricing-manager:
      address: localhost:9093
      negotiation-type: plaintext
